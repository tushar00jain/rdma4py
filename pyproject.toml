[tool.black]
line-length = 88
target-version = ["py39"]

[tool.ufmt]
formatter = "black"
sorter = "usort"

[tool.usort]
first_party_detection = false

[tool.ruff]
line-length = 88
target-version = "py39"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
markers = [
    "integration: exercises real RDMA or storage hardware",
    "gpu: requires a CUDA GPU and GPUDirect RDMA",
]
